import 'package:flutter/material.dart';
import 'package:alertbox/alertbox3801.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: alerlt1(),
    );
  }
}
