import 'package:flutter/material.dart';
import 'package:simpledialod46/simple4601.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: d1(),
    );
  }
}
